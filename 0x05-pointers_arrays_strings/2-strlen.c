#include <stdio.h>
#include <string.h>
/**
 * _strlen - returns length of string
 * @s: character that points to string
 */
int _strlen(char *s)
{	
	char *str
	int len, i;

	str = malloc(sizeof *str * item);
	for(i=0; s[i] != '\0'; i++)
	{
		str[i] = s[i];
	}
	len = sizeof(str);
	return (len);
}
