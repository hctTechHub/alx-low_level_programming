#include "main.h"
#include <stdio.h>
/**
 * main - check the code.
 *
 * Return: Always 0.
 */
int main(void)
{
   	char c;
	c = 'C';
      	_isupper(c);
}
int _isupper(int c)
{   

    
    
    if(isupper(c))
    {
       return (1);
    }
    else
    {
       return (0);
    }
}
